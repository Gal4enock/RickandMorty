const getCharacters = state => state.characters;
const getEpisodes = state => state.episodes;
const getLocations = state => state.locations;
const getWatchList = state => state.watchList;

// eslint-disable-next-line
export default {
  getCharacters,
  getEpisodes,
  getLocations,
  getWatchList
}